A simple Node.js app that demos the Google Calendar API.


1. A JSON Web Token (JWT) client is created using Google APIs.
2. The app uses the JWT to request an access token to use the Google Calendar API.
3. JWT returns the access token.
4. The app uses the access token to make requests to the Google Calendar API.